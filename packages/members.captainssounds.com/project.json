{
  "name": "members.captainssounds.com",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/members.captainssounds.com",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/members.captainssounds.com"
      },
      "configurations": {
        "development": {
          "outputPath": "packages/members.captainssounds.com"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "members.captainssounds.com:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "members.captainssounds.com:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "members.captainssounds.com:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "members.captainssounds.com:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/members.captainssounds.com/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "fetch-secrets": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "doppler secrets download --no-file --format=env-no-quotes -p members-captainssounds-com > packages/angular-members/.env"
          }
        ]
      }
    }
  }
}
